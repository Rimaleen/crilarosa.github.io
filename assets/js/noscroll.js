document.addEventListener('DOMContentLoaded', function() {
    if (lightbox) {
        lightbox.option({
            'resizeDuration': 200,
            'wrapAround': true,
            'disableScrolling': true // blocca scroll automatico
        });
    }
});
